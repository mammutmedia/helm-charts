apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-smart-city-server.fullname" . }}-test-connection"
  labels:
    {{- include "open-smart-city-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-smart-city-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

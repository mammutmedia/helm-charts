apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-github.fullname" . }}
  labels:
    {{- include "nodejs-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-github.selectorLabels" . | nindent 4 }}
